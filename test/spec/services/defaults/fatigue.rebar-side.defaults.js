'use strict';

describe('Service: defaults/fatigue.rebarSide.defaults', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var defaults/fatigue.rebarSide.defaults;
  beforeEach(inject(function (_defaults/fatigue.rebarSide.defaults_) {
    defaults/fatigue.rebarSide.defaults = _defaults/fatigue.rebarSide.defaults_;
  }));

  it('should do something', function () {
    expect(!!defaults/fatigue.rebarSide.defaults).toBe(true);
  });

});
