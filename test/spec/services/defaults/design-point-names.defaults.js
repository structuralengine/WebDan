'use strict';

describe('Service: defaults/designPointNames.defaults', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var defaults/designPointNames.defaults;
  beforeEach(inject(function (_defaults/designPointNames.defaults_) {
    defaults/designPointNames.defaults = _defaults/designPointNames.defaults_;
  }));

  it('should do something', function () {
    expect(!!defaults/designPointNames.defaults).toBe(true);
  });

});
