'use strict';

describe('Service: defaults/sectionShape.defaults', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var defaults/sectionShape.defaults;
  beforeEach(inject(function (_defaults/sectionShape.defaults_) {
    defaults/sectionShape.defaults = _defaults/sectionShape.defaults_;
  }));

  it('should do something', function () {
    expect(!!defaults/sectionShape.defaults).toBe(true);
  });

});
