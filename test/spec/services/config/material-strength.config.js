'use strict';

describe('Service: config/materialStrength.config', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var config/materialStrength.config;
  beforeEach(inject(function (_config/materialStrength.config_) {
    config/materialStrength.config = _config/materialStrength.config_;
  }));

  it('should do something', function () {
    expect(!!config/materialStrength.config).toBe(true);
  });

});
