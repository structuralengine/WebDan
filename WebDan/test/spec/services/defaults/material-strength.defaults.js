'use strict';

describe('Service: defaults/materialStrength.defaults', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var defaults/materialStrength.defaults;
  beforeEach(inject(function (_defaults/materialStrength.defaults_) {
    defaults/materialStrength.defaults = _defaults/materialStrength.defaults_;
  }));

  it('should do something', function () {
    expect(!!defaults/materialStrength.defaults).toBe(true);
  });

});
