'use strict';

describe('Service: defaults/bar.rebarSide.defaults', function () {

  // load the service's module
  beforeEach(module('webdan'));

  // instantiate service
  var defaults/bar.rebarSide.defaults;
  beforeEach(inject(function (_defaults/bar.rebarSide.defaults_) {
    defaults/bar.rebarSide.defaults = _defaults/bar.rebarSide.defaults_;
  }));

  it('should do something', function () {
    expect(!!defaults/bar.rebarSide.defaults).toBe(true);
  });

});
